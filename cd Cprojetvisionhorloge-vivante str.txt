@import "tailwindcss";

:root {
  --background: #ffffff;
  --foreground: #171717;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);

  /* 🌟 Déclaration des 3 rythmes de flottement natifs dans le moteur Tailwind v4 */
  --animate-echo-float-idle: echoFloat 4s ease-in-out infinite;
  --animate-echo-float-speaking: echoFloat 1.2s ease-in-out infinite;
  --animate-echo-float-thinking: echoFloat 0.6s ease-in-out infinite;
  --animate-echo-float-excited: echoFloat 0.4s ease-in-out infinite;

  @keyframes echoFloat {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-6px); /* Amplitude parfaite pour du 32x32px */
    }
  }
}

@media (prefers-color-scheme: dark) {
  :root {
    --background: #0a0a0a;
    --foreground: #ededed;
  }
}

body {
  background: var(--background);
  color: var(--foreground);
  font-family: Arial, Helvetica, sans-serif;
}

/* ─────────────────────────────────────────────────────────────────────────
   ✨ UTILITIES AGENT ECHO (Tailwind v4)
   Halos multicouches en transparence + variations de vitesse de flottement
   ───────────────────────────────────────────────────────────────────────── */

/* 🌌 1. REPOS (Idle) : Flottement très calme, pas de surbrillance */
@utility echo-idle {
  animation: var(--animate-echo-float-idle);
  filter: none;
  transition: filter 0.5s ease;
}

/* 🟠 2. RÉFLEXION (Thinking) : Flottement ultra-rapide + Halo Orange Électrique */
@utility echo-thinking {
  animation: var(--animate-echo-float-thinking);
  border-color: rgba(249, 115, 22, 0.8) !important; /* Orange Tailwind */
  filter: 
    drop-shadow(0 0 4px rgba(249, 115, 22, 0.7))
    drop-shadow(0 0 12px rgba(249, 115, 22, 0.4))
    drop-shadow(0 0 24px rgba(249, 115, 22, 0.2));
  transition: all 0.3s ease;
}

/* 🔵 3. PAROLE (Speaking) : Flottement cadencé + Halo Cyan Profond hyper visible */
@utility echo-speaking {
  animation: var(--animate-echo-float-speaking);
  border-color: rgba(6, 182, 212, 0.8) !important; /* Cyan Tailwind */
  filter:
    drop-shadow(0 0 6px rgba(6, 182, 212, 0.8))
    drop-shadow(0 0 16px rgba(6, 182, 212, 0.5))
    drop-shadow(0 0 32px rgba(6, 182, 212, 0.25));
  transition: all 0.3s ease;
}

/* 🔴 4. PAROLE EXCITÉE / ACTION FINIE (Excited) : Flottement frénétique + Rouge Douce Transparence */
@utility echo-excited {
  animation: var(--animate-echo-float-excited);
  border-color: rgba(239, 68, 68, 0.7) !important; /* Rouge Tailwind doux */
  filter:
    drop-shadow(0 0 5px rgba(239, 68, 68, 0.6))
    drop-shadow(0 0 14px rgba(239, 68, 68, 0.35))
    drop-shadow(0 0 28px rgba(239, 68, 68, 0.15));
  transition: all 0.3s ease;
}